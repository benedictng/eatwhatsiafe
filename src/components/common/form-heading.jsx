import Typography from '@mui/material/Typography';

const FormHeading = ({ heading }) => (
    <Typography variant="h1" sx={{ 'margin-bottom': '48px' }}>{heading}</Typography>
)

export default FormHeading
